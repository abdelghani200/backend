server:
  port: 1010
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/AFTAS
    username: postgres
    password: youcode2023
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
